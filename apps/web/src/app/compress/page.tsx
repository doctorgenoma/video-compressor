import { CompressorPanel } from '../../components/compressor/compressor-panel';

export default function CompressPage() {
  return (<main style={{maxWidth:960,margin:'0 auto',padding:24}}><CompressorPanel /></main>);
}
